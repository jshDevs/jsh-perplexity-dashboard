# jsh-perplexity-dashboard
🚀 JSH Dashboard — Sistema de dashboards dinámicos auto-generados. Laravel 11 + React 18 + TypeScript + Apache ECharts 5 + DuckDB + Redis + PostgreSQL. Self-hosted, offline-first, sin LLM.
